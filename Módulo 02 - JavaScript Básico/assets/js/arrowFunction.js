/* function somar (x, y) {
  return x + y;
} */

const somar = (x, y) => {
  return x + y;
};

//const somar = (x + y) => x + y;

console.log(somar(10, 5));

/* function sobrenome(sob) {
  return "Diego " + sob;
} 

const sobrenome = (sob) => {
  let nomeCompleto = "Diego " + sob;
  return nomeCompleto;
};

const sobrenome = (sob) => {
  return "Diego " + sob;
};

const sobrenome = (sob) => "Diego " + sob;
*/

const sobrenome = (sob) => "Diego " + sob;

console.log(sobrenome("Vieira"));